services:
  postgres:
    image: 'postgres:16-alpine'
    environment:
      - 'POSTGRES_DB=crm_db'
      - 'POSTGRES_PASSWORD=crm_password'
      - 'POSTGRES_USER=crm_user'
    ports:
      - '5432:5432'
    restart: unless-stopped